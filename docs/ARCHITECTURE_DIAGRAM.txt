# 🏗️ Indian Railways MERN Architecture

## Current State: Vanilla JS + DSA

```
┌─────────────────────────────────────────┐
│         BROWSER (Client-Side)           │
├─────────────────────────────────────────┤
│                                         │
│  ┌─────────────────────────────────┐  │
│  │    React Components (Index)     │  │
│  │  - Home (Dashboard)             │  │
│  │  - Booking Form                 │  │
│  │  - Confirmed List (with Sort)   │  │
│  │  - Waiting List                 │  │
│  │  - Status Checker (PNR)         │  │
│  │  - Cancellation                 │  │
│  └─────────────────────────────────┘  │
│                 ↕                      │
│  ┌─────────────────────────────────┐  │
│  │    State Management             │  │
│  │  - actionStack (Stack)          │  │
│  │  - state.confirmed (Array)      │  │
│  │  - state.waiting (Queue)        │  │
│  │  - state.capacity               │  │
│  └─────────────────────────────────┘  │
│                 ↕                      │
│  ┌─────────────────────────────────┐  │
│  │    localStorage (Persistent)    │  │
│  │  - Limited to 5MB               │  │
│  │  - Session-based               │  │
│  └─────────────────────────────────┘  │
│                                         │
│  ┌─────────────────────────────────┐  │
│  │   DSA IMPLEMENTATIONS           │  │
│  │  │                              │  │
│  │  ├─ Stack: Undo (LIFO)          │  │
│  │  ├─ Sorting: 4 options (O(n log n))  │  │
│  │  ├─ Binary Search: O(log n)     │  │
│  │  └─ Priority Queue: Seniors     │  │
│  │                                  │  │
│  └─────────────────────────────────┘  │
│                                         │
└─────────────────────────────────────────┘
```

---

## Future State: MERN Stack (After Setup)

```
DEPLOYMENT ARCHITECTURE
═══════════════════════════════════════════════════════════════

                    ┌──────────────────┐
                    │   Frontend CDN    │
                    │  (Vercel/Netlify)│
                    │  Port: 3000/80    │
                    └────────┬──────────┘
                             │ HTTPS
                             ↓
    ┌────────────────────────────────────────────────┐
    │         BROWSER (React App)                    │
    │  ┌──────────────────────────────────────────┐ │
    │  │  React Components + Hooks                │ │
    │  │  ├─ Navigation (Header + Stats)          │ │
    │  │  ├─ Home (Dashboard)                     │ │
    │  │  ├─ Booking Form                         │ │
    │  │  ├─ Status Checker (PNR lookup)          │ │
    │  │  ├─ Confirmed List (Sorting UI)          │ │
    │  │  ├─ Waiting List (Priority Queue viz.)   │ │
    │  │  └─ Cancellation (with Promotion)        │ │
    │  └──────────────────────────────────────────┘ │
    │  ┌──────────────────────────────────────────┐ │
    │  │  Axios HTTP Client                       │ │
    │  │  baseURL: http://localhost:5000/api      │ │
    │  └──────────────────────────────────────────┘ │
    └────────────┬───────────────────────────────────┘
                 │ HTTP/JSON Requests
                 ↓
    ┌────────────────────────────────────────────────────────┐
    │         EXPRESS SERVER (Backend)                       │
    │  Port: 5000                                            │
    │  ┌──────────────────────────────────────────────────┐ │
    │  │  CORS Middleware                                │ │
    │  │  JSON Parser Middleware                         │ │
    │  └──────────────────────────────────────────────────┘ │
    │                      ↓                                  │
    │  ┌──────────────────────────────────────────────────┐ │
    │  │  ROUTE HANDLERS (10 Endpoints)                  │ │
    │  │  ├─ POST   /passengers            (Book)        │ │
    │  │  ├─ GET    /passengers            (All)         │ │
    │  │  ├─ GET    /passengers/confirmed  (Sorted)      │ │
    │  │  ├─ GET    /passengers/waiting    (Priority)    │ │
    │  │  ├─ GET    /passengers/:pnr       (Binary Srch) │ │
    │  │  ├─ DELETE /passengers/:pnr       (Cancel)      │ │
    │  │  ├─ GET    /stats                 (Info)        │ │
    │  │  ├─ PUT    /capacity              (Update)      │ │
    │  │  ├─ POST   /seed                  (Demo Data)   │ │
    │  │  └─ POST   /reset                 (Clear)       │ │
    │  └──────────────────────────────────────────────────┘ │
    │                      ↓                                  │
    │  ┌──────────────────────────────────────────────────┐ │
    │  │  BUSINESS LOGIC                                 │ │
    │  │  ├─ Booking with Status Check                  │ │
    │  │  ├─ Capacity Management                         │ │
    │  │  ├─ Auto-Promotion from Waiting                │ │
    │  │  ├─ Senior Priority (Age >= 60)                │ │
    │  │  └─ PNR Lookup + Sorting                       │ │
    │  └──────────────────────────────────────────────────┘ │
    │                      ↓                                  │
    │  ┌──────────────────────────────────────────────────┐ │
    │  │  MONGOOSE (MongoDB ODM)                         │ │
    │  │  ├─ Define Schema                              │ │
    │  │  ├─ Add Indexes (for Binary Search)            │ │
    │  │  └─ Query Methods                              │ │
    │  └──────────────────────────────────────────────────┘ │
    └────────────┬───────────────────────────────────────────┘
                 │ MongoDB Query
                 ↓
    ┌────────────────────────────────────────────────────────┐
    │         MONGODB DATABASE                               │
    │  (Local: 127.0.0.1:27017 OR Cloud: MongoDB Atlas)     │
    │  ┌──────────────────────────────────────────────────┐ │
    │  │  railwayDB                                      │ │
    │  │  ├─ passengers Collection                       │ │
    │  │  │  ├─ Documents: { pnr, name, age, ... }      │ │
    │  │  │  ├─ Indexes:                                 │ │
    │  │  │  │  ├─ pnr (unique, for binary search)      │ │
    │  │  │  │  ├─ status (for filtering)                │ │
    │  │  │  │  └─ bookedAt (for sorting)                │ │
    │  │  │  └─ TTL Index: Optional (auto-delete old)    │ │
    │  │  └─ More collections as app grows               │ │
    │  └──────────────────────────────────────────────────┘ │
    │  ┌──────────────────────────────────────────────────┐ │
    │  │  PERSISTENT STORAGE                            │ │
    │  │  ✅ Unlimited capacity                          │ │
    │  │  ✅ Real-time sync across devices              │ │
    │  │  ✅ Scalable to millions of records             │ │
    │  │  ✅ Backup & Recovery options                   │ │
    │  └──────────────────────────────────────────────────┘ │
    └────────────────────────────────────────────────────────┘
```

---

## Data Flow Diagram

### BOOKING FLOW
```
┌─────────────────┐
│  User Submits   │
│  Booking Form   │
└────────┬────────┘
         │
         ↓
┌─────────────────────────────────┐
│  React Booking Component        │
│  - Validates input              │
│  - Prepares JSON data           │
└────────┬────────────────────────┘
         │ axios.post('/passengers')
         ↓
┌─────────────────────────────────┐
│  Express Server                 │
│  - Route: POST /passengers      │
│  - Validates input again        │
│  - Checks available seats       │
└────────┬────────────────────────┘
         │
         ├─── if available ─────────┐
         │                          ├─── status = 'Confirmed'
         ├─── if full ─────────────┤
         │                          ├─ status = 'Waiting'
         │                          └─ Adds to queue
         ↓
┌─────────────────────────────────┐
│  Save to MongoDB                │
│  - Generate PNR                 │
│  - Create Document              │
│  - Return created passenger     │
└────────┬────────────────────────┘
         │
         ↓
┌─────────────────────────────────┐
│  React Updates UI               │
│  - Show PNR                     │
│  - Show Status (Confirmed/Wait) │
│  - Trigger stats refresh        │
└─────────────────────────────────┘
```

### SORTING FLOW
```
User clicks "Sort by Age"
         ↓
┌────────────────────────────────┐
│ Confirmed Component             │
│ setSortBy('age')                │
└────────┬───────────────────────┘
         │
         ↓
┌────────────────────────────────┐
│ fetchConfirmed(sortBy='age')    │
│ axios.get('/passengers/confirmed'
│           ?sortBy=age&order=asc)│
└────────┬───────────────────────┘
         │
         ↓
┌────────────────────────────────┐
│ Express Server                  │
│ - Query: {status:'Confirmed'}   │
│ - Sort: {age: 1}                │
│ - Execute MongoDB query         │
└────────┬───────────────────────┘
         │
         ↓
┌────────────────────────────────┐
│ MongoDB                         │
│ passengers.find()               │
│           .sort({age: 1})       │
│           .toArray()            │
└────────┬───────────────────────┘
         │
         ↓ Return sorted array
┌────────────────────────────────┐
│ React Renders Table             │
│ setPassengers(sortedData)       │
│ Show youngest to oldest         │
└────────────────────────────────┘
```

### CANCELLATION & PROMOTION FLOW
```
User clicks Cancel (PNR: 100001)
         ↓
┌──────────────────────────────────┐
│ axios.delete('/passengers/100001')│
└────────┬───────────────────────────┘
         │
         ↓
┌──────────────────────────────────┐
│ Express Server                    │
│ - Find passenger by PNR           │
│ - Delete from database            │
│ - Check if status was 'Confirmed' │
└────────┬───────────────────────────┘
         │
         ├─ if Confirmed ─┐
         │                 ↓
         │         ┌──────────────────┐
         │         │ Find next from    │
         │         │ waiting list:     │
         │         │ - Age >= 60? YES  │
         │         │ - Then older      │
         │         │ - Pick first one  │
         │         └──────┬───────────┘
         │                 ↓
         │         ┌──────────────────┐
         │         │ Update status to  │
         │         │ 'Confirmed'       │
         │         │ Save to DB        │
         │         └──────┬───────────┘
         │                 ↓
         │         Return: {cancelled,
         │                  promoted}
         │
         ├─ if Waiting ─┐
         │               ↓
         │        Return: {cancelled,
         │                promoted: null}
         │
         ↓
┌──────────────────────────────────┐
│ React Updates UI                  │
│ - Show cancelled info             │
│ - Show promoted info (if any)     │
│ - Refresh lists                   │
└──────────────────────────────────┘
```

---

## DSA Implementation in MERN

### Stack (Undo) - Frontend Only
```
Frontend (React):
├─ actionStack: Array
├─ pushAction(action)
└─ undoLastAction()

Storage:
├─ Session memory
├─ Could persist to backend (POST /history)
└─ Backend could store undo log
```

### Queue with Priority
```
Backend (Express):
├─ Waiting list: MongoDB array
├─ Enqueue logic:
│  ├─ if age >= 60: add to FRONT (priority)
│  └─ else: add to BACK (normal)
├─ Auto-promotion on cancellation
└─ Frontend displays sorted waiting list
```

### Sorting
```
Backend (MongoDB):
├─ Client requests: GET /confirmed?sortBy=age
├─ Server applies: .sort({age: 1})
├─ Database optimizes with indexes
└─ Returns sorted array
Frontend (React):
├─ Renders table with buttons
├─ Triggers different sort queries
└─ Displays results
```

### Binary Search
```
Backend (MongoDB):
├─ PNR indexed (unique)
├─ Query: db.passengers.findOne({pnr})
├─ MongoDB uses B-tree index
├─ O(log n) lookup
└─ Super fast even with millions!

Alternative (Frontend):
├─ If data sorted by PNR
├─ Can implement binary search in React
├─ But MongoDB indexing is better
└─ Leverage database optimization
```

---

## Deployment Topology

```
                        INTERNET
                           ↓
                    ┌──────────────┐
                    │   CDN/Proxy  │
                    └──────┬───────┘
                           ↓
        ┌──────────────────────────────────┐
        │     Deployment Providers         │
        │  (Vercel / Netlify / GitHub)    │
        └──────────────────────────────────┘
                 ↙              ↖
        Frontend (80)     Backend (443)
             ↓                 ↓
    ┌──────────────┐  ┌──────────────────┐
    │React Build   │  │Heroku / Railway  │
    │Dist folder   │  │Express Server    │
    └──────────────┘  └────────┬─────────┘
                                ↓
                    ┌───────────────────────┐
                    │  MongoDB Atlas Cloud  │
                    │  (Database Service)   │
                    └───────────────────────┘
```

---

## Performance Metrics

```
                Vanilla JS    →    MERN Stack    →    Optimized
Storage:        5MB (local)        Unlimited           Unlimited
Speed:          O(n) lookup        O(log n) + cache    O(log n)
Users:          1 session          100+ concurrent     1000+
Reliability:    No backup          Daily backups       Real-time
Scaling:        Not possible       Horizontal          Auto-scale
Cost:           Free               $10-50/mo           Pay-as-you-go
```

---

## Technology Stack Breakdown

### Frontend Stack
```
React 18
├─ Vite (Fast build tool)
├─ React Hooks (Functional components)
├─ Axios (HTTP client)
├─ CSS3 (Responsive design)
└─ Browser APIs (localStorage, fetch)
```

### Backend Stack
```
Node.js v16+
├─ Express.js (Web framework)
├─ Mongoose (MongoDB ODM)
├─ CORS (Cross-origin requests)
├─ dotenv (Environment variables)
└─ nodemon (Dev server auto-reload)
```

### Database Stack
```
MongoDB
├─ Cloud: Atlas (recommended)
│  └─ mongodb+srv://user:pass@cluster.mongodb.net
└─ Local: Community Edition
   └─ mongodb://127.0.0.1:27017
```

---

**Architecture Complete!** 🏗️

*Vanilla JS: Single-tier (Browser only)*
*MERN: Three-tier (Frontend → Backend → Database)*

